# 402cap — Org Backlog

**Work top-down.** Skip anything marked BLOCKED. Update status in place; append new items at the
bottom of their section. Read `ORG-LESSONS.md` before dispatching so a known dead end isn't retried.

Status: `open` · `building` · `shipped` · `parked` · `blocked`

---

## P0 — the moat depends on these

| # | Item | Why it matters | Status |
|---|---|---|---|
| 1 | **Daily automated snapshot** | The entire moat is time-series. Every day we don't snapshot is a day of history that can never be recovered — Coinbase overwrites quality metrics every 6h and drops resources after 30 days. This is the single highest-value item on the board and it is pure plumbing. Headless scheduled task, no console. **First attempt fired 0/10 (2026-07-30): no scheduled task, no artifact, no report.** Second pass registered the task for real (`x402-daily-snapshot`, cron `0 3 * * *`, enabled) — confirmed via `list_scheduled_tasks`. **RESOLVED 2026-07-31 Sprint 5 (Data), root cause found — it was NOT a silent abort, it was a slow-but-real partial run caught mid-flight:** the 08:08 UTC investigation started while the cron's own session was still in-flight (proof: a fresh `voicemode-mcp-launcher` process pair spawned at exactly 03:08:01 local — a session-start signature — showing the scheduler really did launch a Claude session at the scheduled time). ~18 minutes of session overhead later it wrote snapshot 7 (08:27:01Z), ran probe run 7 (400/400 stored, 08:29:18Z), and report (08:29:37Z) — all real, all verified against the live DB, not a log claim. It then **stopped without ever reaching steps 5 (prune.py) or 7 (git commit)**: log filenames prove catalog/probe/report ran as **three separately-stamped invocations** (`catalog-20260731-032701.log`, `probe-...-032854.log`, `report-...-032936.log` — three different `$Stamp` values, so NOT one `run.ps1 daily` call, which stamps once), then nothing further. Whether the session hit a turn/context limit or self-declared done after step 6 is not determinable from disk — say so rather than guess. No artifact/log recorded any of this until now because nothing in the SKILL wrote a line on any exit path. Fixed: SKILL.md now requires (a) a START line and a per-step completion line in `logs/daily-snapshot-cron.log`, so a truncated run names its own stopping point without needing the dying session's cooperation, and (b) a note that `run.ps1`'s "daily" verb specifically skips prune/commit if ever used as a shortcut (true and worth flagging, independent of what actually ran today). Manually ran the missing steps this cycle: `prune.py` (0 rows moved — DB was already deduped from the earlier sprint-4 pass, 0 verification failures), `report.py`, `mismatch_report.py` (all exit 0). **Before: `MAX(catalog_snapshot.id)`=6 (03:07 UTC). After: id=7 (cron, 08:27:01Z, real, 3,077 change_events, the real day's churn) and id=8 (an extra snapshot I created by racing my own manual `fetch_catalog.py` against the still-running cron session before realizing it was live — only 1 change_event vs 7, 46s later).** **Side effect, not yet cleaned up:** `v_latest_catalog` partitions by MAX(snapshot_id), so "latest" now resolves to snapshot 8 — any consumer reading "today's churn" off latest-vs-previous will see snapshot 8's near-empty diff, not snapshot 7's real 3,077-event churn, until this is reconciled. Did not `git commit` — `git status` showed `x402_common.py`/`fetch_catalog.py` already modified. **Correction, sprint 6 recording: this was misattributed as "other in-flight sprint work I was told not to touch."** `git diff` shows it is in fact this same Data department's own Backlog #18 (source-partitioning) work from earlier in this same sprint, not an external untouchable process — Council review caught this. The underlying reason to skip a blanket `git add -A` still holds (a real uncommitted diff existed, mid-sprint, from a sibling task), just not for the stated reason. Both diffs and this backlog file were committed together at end of sprint 5 recording. Live-verified 2026-07-31 sprint 6 recording: `catalog_snapshot` MAX(id)=8 (08:27:47Z), `change_event`/`resource_dim` both show `source='cdp'` for all rows, `PRAGMA integrity_check`=ok, scheduled task confirmed via `list_scheduled_tasks` (`enabled: true`, `lastRunAt: 2026-07-31T08:08:00Z`, `nextRunAt: 2026-08-01T08:07:57Z`), `logs/daily-snapshot-cron.log` confirmed present and non-empty (the SKILL.md logging fix is real, not just claimed). | **partially verified — unattended snapshot+probe+report confirmed against the live DB; unattended prune/commit still not observed; snapshot-8 churn-masking side effect open; next fire 2026-08-01T08:07:57Z now logs either way** |
| 2 | **Run `prune.py` after the full sweep completes** | Measured: 67.4% of `raw_json` is byte-identical duplication; 79MB reclaimable now, ~26GB/year at daily cadence. Written and documented, NOT yet run — it takes a whole-db write lock, so it must not run during a fetch/sweep. Verify on a copy first. | **shipped — status was stale, corrected 2026-07-31.** `logs/prune.log` (local 01:02) shows it already ran against prod: 47,570 rows moved into `raw_blob` (15,567 distinct bodies), 0 verification failures, 395.3MB → 293.6MB (101.8MB reclaimed). Confirmed live against the DB itself, not just the log: `raw_blob` currently holds exactly 15,567 rows, matching the log. |
| 3 | **Cross-facilitator coverage** | Coinbase indexes only its OWN settlements, so anything settling via x402.org / xpay.sh / payai.network is invisible to it — and it can never fix that, because it is a facilitator. This is the part of the moat Coinbase structurally cannot copy. Enumerate other facilitators' catalogs and merge into our index with a `source` column. | researched — see `notes-cross-facilitator.md`. 4/19 facilitators have live keyless discovery (CDP, PayAI, UVDAO, Thirdweb); measured 808 hosts (86.2%) invisible to CDP across the 3 non-CDP ones. Schema designed, NOT migrated — ingestion code is the next open item. |

## P1 — the product

| # | Item | Why it matters | Status |
|---|---|---|---|
| 4 | **`builder-code` referral tracking** | Backfill APPLIED to production 2026-07-31: schema had drifted (`raw_json` deduped into `raw_blob` by `prune.py`, script fixed to read `catalog_resource_full.raw_json_full`) — re-verified against a copy first, backed up prod (`x402_index.db.bak-2026-07-31-preref`), applied, independently re-verified. **Real counts: `referral_json` set on 8,107 rows, `referral_event` seeded with 2,670 (program='builder-code'), 0 duplicate pairs, `PRAGMA integrity_check`=ok.** Concentration analysis re-run with real queries: top code `bc_gxy6qn5p` still 59.4%/1,587 routes (2 registrable domains), top-2 = 79.1%, identical across all 3 complete snapshots (no real trend yet — same-day fetches). Builder-code presence correlates with a small median traffic bump (2 vs 1 calls) but NOT a mean bump once the single dominant host (`x402.twit.sh`) is stripped. Curated correlation: still 0/122 overlap. Revenue-share: NOT ESTABLISHED (Base frames it as protocol-discretionary). See `notes-builder-code.md` §7-8. | **shipped — see Done** |
| 5 | **Public site — MVP** | The CMC-style front end: searchable listing table, per-resource detail with price history chart, "what changed today" feed. Check `SALVAGE-FROM-CELESTIALS.md` for liftable frontend scaffolding before building from scratch. | **shipped — see Done** |
| 6 | **Read API + MCP server** | Michael's stated endgame: "the api for agent workers to use our api to scan for price changes." MCP is how agents consume tools today. Two endpoints carry the product: *what changed since T* and *history for resource X*. | **shipped — see Done** |
| 7 | **Price-mismatch monitor** | Measured 3.4–7.7% of catalog prices are WRONG vs. live — one off by 400× with a `lastUpdated` an hour old. This is the single most demonstrable proof that measured beats declared. Make it a standing metric with a leaderboard of worst offenders. | **shipped — see Done (2026-07-31, Intel productization).** Severity-tiered (CRITICAL/HIGH/LOW) signal: `/mismatches` API filters by severity, `mismatch_report.py` wired as step 6b of the daily cron *by code* — but per Backlog #1, the cron's actual unattended completion is unverified, so "regenerates every snapshot" is the design intent, not yet an observed fact. **Updated 2026-07-31 Sprint 5:** `mismatch.json` regenerated against the current 400-route sample (post-Backlog #1 verification run) — comparable=376, mismatches=16 (4.3%), 3 CRITICAL. This is a smaller basis than the 721-mismatch figure quoted elsewhere (that was a fuller sample against snapshot 6); the two numbers are not comparable sample sizes and should not be read as a trend. Whatever `mismatch.json` says at any moment is basis-dependent on the probe sample that produced it — this cell will go stale again the next time it regenerates, by design. |
| 11 | **Fix `v_builder_codes`/`v_latest_catalog` to filter `is_complete`** | Surfaced by P1#4 rework: neither view filters incomplete snapshots, which would silently corrupt any read API (#6) or referral dashboard built on top. Small fix, must land before #6 touches these views. **REOPENED-then-actually-fixed 2026-07-31 (Data, source migration cycle): the "shipped" status was WRONG at the schema level — verified against the live `sqlite_master.sql`, `v_latest_catalog`'s stored definition had no `is_complete` filter at all. What had actually shipped was a Python-side workaround in `api/db.py` only (`LATEST_CATALOG_CTE`); `build_site.py`, `report.py`, and `v_builder_codes` itself were still reading the unfixed view. Now genuinely fixed at the view (see #17).** | **shipped — see Done (real fix, #17)** |
| 15 | **Second independent method for the on-chain dollar-value figure** | Chain dept's $0.55M–$2.98M extrapolation rests on one method (Transfer-log decoding) over one ~200s window, n=150. Council-flagged gap. | **shipped — see Done** |
| 16 | **Fix index.html headline mismatch count (726) vs mismatch.html's validated count (721)** | Council-flagged self-contradiction: index.html counts raw `price_mismatch=1` rows including 5 templated routes; mismatch.html correctly excludes them per its own stated methodology. Same snapshot, two different numbers, one click apart, on a measured-truth product. Fix: exclude `is_templated` rows from the index.html stat too, or label it "includes templated, see mismatch.html for validated count." **REOPENED 2026-07-31 cycle 3 review: claimed-fixed but NOT fixed** (badge render still 726 vs headline 721). **CLOSED FOR REAL 2026-07-31 sprint 4 (Product-rework, 9.2/10 ACCEPT):** `mismatch_badge(price_mismatch, is_templated)` added to `build_site.py` (~L215), mirrors `alive_badge`, returns `""` for templated rows; both call sites fixed (detail page ~L288, index table ~L370). Detail-page `<meta description>` (~L350) now says "not tested (templated route)" for templated routes instead of asserting mismatch/no-mismatch. Site regenerated (15,524 rows, 6.4s). Independently reverified this cycle: `h.count('badge badge-warn')` on the fresh `site/index.html` = **721**, matching the headline exactly. Templated-route detail page (`api.influship.com/.../:var1`) confirmed carrying the new disclosure text and zero `price mismatch` badge spans. | **shipped — see Done** |

## P2 — distribution and authority

| # | Item | Why it matters | Status |
|---|---|---|---|
| 8 | **"The x402 economy is 9× smaller than reported" analysis** | Derived GMV is $62.8k/30d, but the top 4 routes are 82% of it — invoice/checkout endpoints with 1–28 lifetime calls, one a single call priced at $10,000. Real recurring activity is ~$7k/mo across 5,016 routes. Nobody else can show this. It is the launch story that earns authority. **Publishing anything public is Michael's call** — produce it as a draft. | open |
| 9 | **Dead-pool / graveyard dataset** | Coinbase deletes resources after 30 days without settlement. We keep them. "What died" is real market intelligence that literally cannot be bought anywhere else. Needs #1 running to accumulate. | open |
| 10 | **Rank the real earners** | Strip self-payers and outliers, publish an honest top-100 by recurring revenue. Requires a real-payer heuristic — `l30DaysUniquePayers` may count the operator self-registering (UNVERIFIED, see README §10). | open |
| 12 | **Ingest the 3 confirmed non-CDP facilitator catalogs (PayAI, UltravioletaDAO, Thirdweb)** | Coverage is measured (808/937 hosts, 86.2%, invisible to CDP) but no ingestion code exists yet and the `source` column is designed, not migrated. This is what actually captures the cross-facilitator moat rather than just proving it exists. See `notes-cross-facilitator.md` for the schema plan. | open |
| 13 | **x402scan.com discovery API** | Real resource/facilitator data behind a genuine unpaid 402 ($0.01/call) — could add another facilitator's view but paying, even $0.01, needs Michael's sign-off first (hard line: no purchases without him). Parked until he says spend it. | parked — needs Michael |
| 14 | **xpay.sh 1,089-tool catalog** | Real data but MCP-wrapped with zero per-tool URL/host fields — doesn't fit the per-resource schema as-is. Would need a distinct ingestion path (tool-identity keyed, not URL-keyed) if ever pursued. Not started. | open, low priority |
| 17 | **Multi-source schema migration** | `source` column added to `catalog_snapshot` + `catalog_resource` (DEFAULT 'cdp', additive, verified metadata-only). `v_latest_catalog` fixed at the schema level (is_complete filter + (source,resource_url) partition) — genuinely closes #11, not just the API-layer workaround. See `notes-source-migration.md` for full verification (before/after diff on real codebase queries, positive control on the is_complete fix, idempotency proven by running twice on prod). Backup: `x402_index.db.bak-2026-07-31-presourcemig`. **NOT done, named explicitly**: `change_event`/`resource_dim` source-partitioning (needed once a 2nd source's rows land, harmless today with one source) and actual non-CDP ingestion code — both are Ecosystem's #12. | **shipped — see Done** |
| 18 | **Source-partition `change_event` and `resource_dim`** | `source TEXT NOT NULL DEFAULT 'cdp'` added to both tables (additive ALTER TABLE, 0.0114s on the real 302MB prod file, timed not guessed). Found and fixed a real bug this migration would otherwise have caused: `fetch_catalog.py::rebuild_resource_dim()`'s `INSERT INTO resource_dim SELECT ...` had no column list (positional) — reproduced the exact break on a scratch copy (`table resource_dim has 8 columns but 7 values were supplied`) before fixing it with an explicit column list. `change_event`'s INSERT already named its columns, so needed no code change. Verified: `report.py` output byte-identical before/after on real prod (via `git stash` to capture true pre-migration behavior); `PRAGMA integrity_check`=ok before/after; `connect()` re-run on prod with no error (idempotent). Full detail in `notes-source-migration-2.md`. Backup: `x402_index.db.bak-2026-07-31-sourcepart2`. **NOT done, named explicitly**: `resource_dim`'s PRIMARY KEY is still `resource_url` alone (not `(source, resource_url)`), and `diff_snapshots()`/`rebuild_resource_dim()`'s `GROUP BY`/lookup logic is still keyed on `resource_url` alone — so the schema is now safe for a 2nd source's rows to exist, but `resource_dim` rebuild and diff logic will still silently collapse/cross-compare two sources' observations of the same URL string until that keying work is done. That data-model fix, plus actual non-CDP ingestion, remains Ecosystem's #12. | shipped — see Done, #12 still blocked on the keying gap above |

## Blocked on Michael — do not attempt

| # | Item | What it needs | Status |
|---|---|---|---|
| B1 | Buy a domain (`402cap.com` recommended, verified available) | ~$11/yr on his card. Raise as a Paybox request. | blocked |
| B2 | CDP account for mainnet facilitator + Bazaar listing | His signup, his API keys. | blocked |
| B3 | Self-pay once to bootstrap our own Bazaar listing | Real USDC + gas. Paybox request; he approves. | blocked |
| B4 | Publishing anything public under his name | Drafts only until he says go. | blocked |

---

## Done

| Item | Outcome | Status |
|---|---|---|
| Rebuild `x402-endpoint` into a spec-compliant v2 seller | Was unpayable (invented `?x402_receipt=` flow); now emits a real 402. Two security bugs (free SSRF oracle, DNS-rebinding TOCTOU) closed by test. | shipped |
| Catalog fetcher + unpaid prober + churn diffing | 15,524 routes / 1,577 hosts. 96.7% alive, 320ms median. | shipped |
| Protocol research + `IMPLEMENTATION-SPEC.md` | 9 agents, adversarially verified. Read it before any x402 work. | shipped |
| Version control | Both repos under local git. GitHub deferred until deploy; private repo when it happens. | shipped |
| Lift backlog tooling from Celestials | `backlog_index.py` + `backlog_merge.py` repointed at this repo. Selftest 26/26. Gives the org the file-based handoff the charter assumes. | shipped |
| **Chain — verify the dollar figure (Phase 1 #1)** | `notes-chain-values.md` + `onchain_verify_values.py`. 150/150 sampled Coinbase facilitator tx confirmed genuine `transferWithAuthorization` calls (positive control passed first). Extrapolated real settlement value: **$0.55M–$2.98M, not the claimed $28.6M/$52.6M (10–52× too low)**. 8.3/10 ACCEPT — single method/single ~200s window, second method not yet done (→ Backlog #15). Do not quote either figure as final. | shipped, partially verified |
| **Intel — reconcile the three-way contradiction (Phase 1 #5)** | `notes-reconciliation.md`. DB total (373,056/30d) reverified exact. Bridged to on-chain via two disjoint 500-block windows ~12h apart: 4.73M–6.26M facilitator-sent tx/30d, 99.2–99.8% confirmed genuine settlements. **Headline: CDP catalog captures ~6–8% of Coinbase's real settlement volume; >92% uncatalogued.** (c) vs (b) (x402scan resource counts) left honestly NOT ESTABLISHED. 9.2/10 ACCEPT. Flagged an orphaned Chain-dept process still running at audit time — confirmed gone as of this cycle's process check. | shipped |
| **Product — public site MVP (Phase 2/3)** | `build_site.py` → `site/` (134MB, 15,527 static HTML files, no CDN/build tool, 6.2s build, 0 filename collisions). Index (search/sort/filter) + per-resource detail pages + `mismatch.html` leaderboard (top: claudelines.com, 20,000,000×). Honesty features: 3-state "alive", no-decimals-assumed price rendering, $52.6M explicitly excluded from headlines. 8.5/10 ACCEPT — index.html's headline count (726) vs mismatch.html's validated count (721) disagree by the 5 templated rows → Backlog #16. | shipped, minor fix pending |
| **Product — fix index/mismatch mismatch-count disagreement + add coverage.html (Backlog #16 + hardening)** | **PARTIALLY SHIPPED, headline claim was wrong.** `site/coverage.html` genuinely shipped and is good (CDP catalog = 3.0% of real 30d transactions / 8.8% of real dollar volume, both methods shown, caveats intact — independently confirmed on disk). But the #16 fix did NOT work: `build_site.py:370` (`mismatch_badge`) has no `is_templated` guard, unlike `alive_badge` right next to it — only the headline stat (line 383) was fixed. Re-counted the regenerated `site/index.html` this cycle: **726** `badge-warn "price mismatch"` spans still render in the table, against a headline that says 721. Same contradiction the item was opened to kill, moved one scroll down. 4/10, RECOMMEND_FIRING. Backlog #16 reopened. | **shipped (coverage.html only) — #16 itself reopened** |
| **Referral — apply builder-code backfill to production** | Caught a schema-drift bug before it caused a silent zero-row no-op (`raw_json` deduped into `raw_blob` post-prune, script fixed to use `catalog_resource_full`). Tested against a copy, backed up prod, applied, independently re-verified: `referral_json` set on 8,107 rows, `referral_event` seeded 2,670/2,670 no-dupe. Concentration/traffic/curated correlation re-tested with real queries, not asserted. See `notes-builder-code.md` §7-8. | shipped |
| **API/MCP — the read API agents consume (Phase 2/3)** | `api/{db.py,queries.py,main.py,mcp_server.py,requirements.txt,README.md}`. FastAPI + MCP server (`mcp==1.29.0`) over read-only SQLite. Fixed Backlog #11 (`is_complete` filter bug) with a positive-control proof (fake incomplete snapshot correctly excluded). Endpoints verified against live 15,524-route DB headless (`pythonw.exe`, port confirmed closed after). MCP verified only via in-process `list_tools`/`call_tool`, not a real transport — disclosed, not glossed over. 9/10 ACCEPT. | shipped |
| **Chain — second independent method for the dollar figure (Backlog #15)** | `notes-chain-values-method2.md` + `onchain_verify_values2.py`. Positive control on calldata decoder: 4/4 PASS on our own known $0.01 txs (caught and fixed a false FAIL from checking the wrong field first). Decoder cross-check vs. method 1's log-decoded values: 150/150 exact match (validates the decoder, not the magnitude). Real second method: disjoint 100-block window 12h before method 1's, full-population decode (n=573, not a subsample) — median $0.017149 vs. method 1's $0.00515, explained by mixture share not price. **Strongest finding: the single mega-merchant (75.7% of x402scan's 30d tx) is independently corroborated three ways** — measured $0.0157–0.0182 median across both our windows vs. x402scan's own stated $0.0169/tx. Honest open gap: the non-mega tail measured ~6× below what x402scan's aggregate implies for it — flagged, not resolved. 30d Coinbase-dollar extrapolation via Intel's bridged tx-count range: **only the mean basis is quotable, $132,393–$394,355**; the median basis ($24K–$394K as originally written here) is INVALID — median×count, corrected in this cell 2026-07-31 sprint 6 after a Council review caught STRATEGY.md still carrying the unflagged figure. Still nowhere near the refuted $28.6M/$52.6M either way. Small 30d-back spot check (n=42) confirms same fat-tailed shape a month apart. No orphaned process after completion (checked). | shipped |
| **Data — multi-source schema migration + real `v_latest_catalog` fix (Phase 2 #17)** | `notes-source-migration.md` + edits to `x402_common.py`. `source` dimension added (both tables, additive, DEFAULT 'cdp', metadata-only ALTER confirmed at 0.06s on the real 294MB prod file). Diffed 8 real codebase queries before/after: byte-identical hashes. The one query that changed (`SELECT * FROM v_latest_catalog`) was proven column-for-column identical except the new `source` col (15,524 rows, 0 mismatches). Positive control on the `is_complete` fix: synthetic incomplete snapshot poisoned a real row with `$999999.99`; old view logic returned the poison, new view correctly didn't. Idempotency: ran `connect()` twice on prod, both clean, `PRAGMA integrity_check`=ok. Found Backlog #11 was marked `shipped` but the live schema proved it wasn't — only `api/db.py` had a Python-side workaround; the stored view was still broken for `build_site.py`/`report.py`/`v_builder_codes`. Deliberately not done: `change_event`/`resource_dim` source-partitioning (Ecosystem's ingestion work, harmless today with one source). | shipped |
| **Intel — merchant concentration deep-dive (0xe9030014f5dae217d0a152f02a043567b16c1abf)** | `notes-merchant-concentration.md` + `onchain_merchant_concentration.py`. Identified as BlockRun.AI (agent-tool API reseller), catalogued in CDP but the catalog's own counters capture only ~1/3,400 of its real on-chain tx (2,715 catalogued calls/30d vs 9,401,793 real) — coverage-gap vs. counting-bug left open. Confirmed bursty volume on 10 disjoint windows (16–19x spikes recur, not a one-off), aggregate rate ~12.29M/30d corroborates x402scan's 9.4M/30d order of magnitude. Binary-searched first-nonzero-balance block: merchant is only ~7.1 months old (funded 2025-12-26). Confirmed via genesis-block timestamp Base itself is only 37.5 months old — the 74-month implied all-time duration is mathematically impossible for the Coinbase/Base portion; ramp-up bias flagged as leading unproven hypothesis. Dollar-share (22.3%) explicitly flagged as vendor-derived (x402scan's $0.0169), not independently confirmed. First draft over-claimed a balance≈revenue "corroboration" on an n=1 burst sample — caught on advisor review, re-sampled to n=10 and re-run to confirm no dup/range artifact, claim corrected before shipping. | shipped |
| **Product — Backlog #16 real fix: mismatch-badge/headline contradiction closed (sprint 4)** | `build_site.py`. `mismatch_badge(price_mismatch, is_templated)` added (mirrors `alive_badge`), both call sites (detail ~L288, index table ~L370) fixed; detail-page meta description (~L350) guarded to say "not tested (templated route)" for templated rows; `coverage.html` gained a sentence reconciling 3.0%/8.8% (CDP vs all-facilitators) against the older ~6-8% (CDP vs Coinbase-only) figure. Site regenerated headless (15,524 rows, 15,527 pages, 6.4s). 9.2/10 ACCEPT. Re-verified independently this cycle: `site/index.html` `badge badge-warn` count = 721 (matches headline exactly, was 726 before the fix); templated-route detail page confirmed carrying the new disclosure text with zero mismatch-badge spans. Council-noted gap (not a blocker): report didn't itself cross-check 721 against the DB's raw `price_mismatch` count, and didn't proactively test the 5 templated+mismatch=1 edge-case rows as its own proof (Council did both; both hold). | shipped |
| **Intel — productize the price-mismatch feed into a standing severity-tiered signal (Phase 2, Backlog #7)** | `mismatch_report.py`, `api/queries.py`, `api/main.py`, `api/mcp_server.py`, `run.ps1`, `~/.claude/scheduled-tasks/x402-daily-snapshot/SKILL.md`. Added `high_count`/`low_count` + machine-readable `severity_definition` to `mismatch_report.py`'s meta (721 mismatches, 13 CRITICAL, 483 HIGH, 225 LOW, unchanged). `/mismatches` now reads `mismatch.json` directly instead of running its own divergent ratio-only SQL severity calc — closes the exact "two definitions of one number" bug class Backlog #16 already burned this org on. Found+fixed a real `ZeroDivisionError` in the ported `min_ratio` filter (free route, ratio=0.0, `1/ratio` crashes) by actually calling the function. Added `severity=CRITICAL|HIGH|LOW` filter to the API and MCP server. Wired `mismatch_report.py` as step 6b of the daily cron (`x402-daily-snapshot`, confirmed registered and enabled). **Correction on re-verification:** the cron's `lastRunAt` alone is not proof step 6b has ever actually executed unattended — see Backlog #1 (reopened, no new snapshot exists despite a claimed fire). The code path is real and was manually exercised end-to-end this cycle; automatic recurring execution is not yet observed. 9/10 ACCEPT. | shipped |
