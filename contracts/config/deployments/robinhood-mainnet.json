{
  "chainName": "robinhood-mainnet",
  "chainId": 4663,
  "deployedAt": "2026-09-05T08:47:02Z",
  "deployBlock": 54989195,
  "startBlock": 54989143,
  "deployedAtNote": "TWO BLOCKS, and the record names which instant it publishes. The oracle went in first, alone, in block 54,989,143 (own timestamp 1788598017 = 2026-09-05T08:46:57Z, read with `cast block`); the six singletons and the three wire calls landed together five seconds later in block 54,989,195 (own timestamp 1788598022 = 2026-09-05T08:47:02Z). `deployedAt` and `deployBlock` are the SECOND block — the instant the protocol became wired and complete. `startBlock` is the FIRST, because an indexer that starts at the second one misses the oracle's own deploy. Both numbers are block timestamps, not the runner's wall clock; the Base Sepolia record carries a note about a one-second wall-clock drift that this record avoids by never reading the clock.",

  "deployer": "0x0f80606a2283fD9C67cE2eEC79B90E95907F9f35",
  "sourceCommit": "b1cde122",
  "rpc": "https://rpc.mainnet.chain.robinhood.com",
  "explorer": "https://robinhoodchain.blockscout.com",

  "note": "The Robinhood Chain mainnet (chain 4663) deployment, ten transactions, every receipt status 1. `sourceCommit` b1cde122 is the tip of origin/protocol/main and is the tree both broadcast scripts were built from. WHAT WAS READ, AND WHY IT WAS READ RATHER THAN COPIED: every address, every wiring edge, every oracle field and every code size below was read from chain 4663 with `cast` at block 54,991,182, pinned with `--block` so the whole table is one consistent state. The broadcast JSON was NOT trusted for any of it, and on this run that was not a precaution but a correction — see `broadcastLabelWarning`. `contracts/config/robinhood-mainnet.json` was likewise not trusted: it is a configuration file, it says of itself that nothing is deployed with it, and it was written the day before this broadcast. A configuration records what someone intended; this file records what the chain returned.",

  "readBackAt": {
    "block": 54991182,
    "how": "`cast call --block 54991182` against https://rpc.mainnet.chain.robinhood.com, 1,987 blocks after the deploy. `cast chain-id` on that endpoint answers 4663.",
    "date": "read 2026-09-05"
  },

  "broadcastLabelWarning": "THE BROADCAST JSON'S `transactions[].hash` COLUMN IS SCRAMBLED ON THIS RUN, and anyone rebuilding this table from `contracts/broadcast/Deploy.s.sol/4663/run-latest.json` will get seven of the nine core rows wrong. Worked example: that file pairs contractName `SubVaultRegistry` with hash 0x653b9df1…, while `cast receipt 0x653b9df1…` returns contractAddress 0xE200d63D… — the OperatorRegistry. The same file's `receipts[]` array is CORRECT (its hash-to-contractAddress-to-gasUsed rows agree with the chain in all ten cases), so the two halves of one file disagree with each other. This is the failure mode `scripts/soak/deployment.mjs`'s header records from Sprint 9, where taking the labels at face value would have swapped VaultFactory and VaultDeployer; it recurred here. The `transactions` table below is built from `cast receipt`, and the contract identities are independently established by `bytecodeCurrency` rather than by any label.",

  "transactions": {
    "note": "hash, block, gasUsed and txIndex from `cast receipt` for each of the ten hashes; status 1 on all ten. Ordered by execution. `txIndex` is the position within the block and is recorded because the six singletons are not contiguous — other traffic sat between them.",
    "totalGasUsed": 11062629,
    "deploys": [
      {"contract": "ChainlinkOracle",  "address": "0x79279FBa3b6F6736f07cbBFcB7Cf0559466D5bfB", "hash": "0x13162f0bb9517f20b038b9b0f3efb7ab3d6fd6eb43602df367703cbebadad8de", "block": 54989143, "txIndex": 8,  "gasUsed": 571098,  "status": 1},
      {"contract": "OperatorRegistry", "address": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568", "hash": "0x653b9df1a1b03df63ad4cf32e3b88834130ed0d397d3984d790659eb5b0196c4", "block": 54989195, "txIndex": 1,  "gasUsed": 532777,  "status": 1},
      {"contract": "SubVaultRegistry", "address": "0x692385262C05df7515560886f167c4eDD0814025", "hash": "0xe0eb4c09ad6d6b0bd2c382a2a29f44a6b37ad1c6eec26c40ec5bb7f895cd3a6e", "block": 54989195, "txIndex": 2,  "gasUsed": 400053,  "status": 1},
      {"contract": "FeeEngine",        "address": "0x221D09326DBf6CDb708E7aBEdC9B117d64Ac4232", "hash": "0x42cc8ecb406730a82b8e37591b40816d68e2140a2c145cf646d1c94de44aa038", "block": 54989195, "txIndex": 4,  "gasUsed": 703792,  "status": 1},
      {"contract": "Governance",       "address": "0x790A308f1ac06FeD4C79884BAD25d0C721C5B125", "hash": "0xba0d99bcde5b4c53600f6cd554d9bd23043fdf16ed54112491b3ef5ba67bc001", "block": 54989195, "txIndex": 5,  "gasUsed": 2679326, "status": 1},
      {"contract": "VaultDeployer",    "address": "0xc36198FD2c7C62738159ED1FF965679105FAF05a", "hash": "0xf551bd8da4440c6f569842e42093b54bf2541d5d52493dff4afecf46ba353c13", "block": 54989195, "txIndex": 11, "gasUsed": 5159604, "status": 1},
      {"contract": "VaultFactory",     "address": "0xc44B853F037b4fF33B831C9a2B341686dEC88Fd1", "hash": "0x655991c787a11fba126f7ce93608c6d76f530da3576a834f06f30f442cbce343", "block": 54989195, "txIndex": 12, "gasUsed": 858256,  "status": 1}
    ],
    "wiring": [
      {"call": "OperatorRegistry.wire(VaultFactory, FeeEngine)", "to": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568", "hash": "0xc8cc8cfba6c8928baee3a88106d6ef19f5cfdbd1d51f4c477b691c824f8e0f95", "block": 54989195, "txIndex": 13, "gasUsed": 69074, "status": 1},
      {"call": "SubVaultRegistry.wire(VaultFactory)",            "to": "0x692385262C05df7515560886f167c4eDD0814025", "hash": "0x38a2de1bf65ddeb77bb07e9f97a5d55a6ebdb1880085567ec1c8567986b50933", "block": 54989195, "txIndex": 14, "gasUsed": 44076, "status": 1},
      {"call": "Governance.wireSubVaultRegistry(SubVaultRegistry)", "to": "0x790A308f1ac06FeD4C79884BAD25d0C721C5B125", "hash": "0xec6000afb917ec99612a3afe783e634a134da5eaa40de069fa09a224ee99bee3", "block": 54989195, "txIndex": 16, "gasUsed": 44573, "status": 1}
    ]
  },

  "singletons": {
    "OperatorRegistry": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568",
    "SubVaultRegistry": "0x692385262C05df7515560886f167c4eDD0814025",
    "FeeEngine": "0x221D09326DBf6CDb708E7aBEdC9B117d64Ac4232",
    "Governance": "0x790A308f1ac06FeD4C79884BAD25d0C721C5B125",
    "VaultDeployer": "0xc36198FD2c7C62738159ED1FF965679105FAF05a",
    "VaultFactory": "0xc44B853F037b4fF33B831C9a2B341686dEC88Fd1"
  },

  "bytecodeCurrency": {
    "claim": "All seven deployed contracts, and the VaultCore creation code the VaultDeployer carries, are byte-for-byte what sourceCommit b1cde122 builds. Nothing here rests on a code SIZE comparison.",
    "basis": "Two independent byte-level checks, both run 2026-09-05 against the artifacts in the b1cde122 build tree (`contracts/out`, built in a worktree whose only untracked file is a deploy helper). (1) RUNTIME: `cast code <addr> --block 54991182` compared byte-for-byte with each artifact's `deployedBytecode.object`. Every differing byte falls inside a window the artifact itself declares in `deployedBytecode.immutableReferences` — that is, inside a constructor-set immutable, which an artifact necessarily leaves as zeros. Bytes differing OUTSIDE those windows: 0, 0, 0, 0, 0, 0, 0. (2) CREATION: `cast tx <hash> input` for each of the seven deploys begins with that artifact's `bytecode.object` as an exact prefix, with only ABI-encoded constructor arguments following. The two checks are independent — one reads what is on the chain now, the other reads what was submitted — and they agree.",
    "whyThisAndNotCodesize": "A codesize table would have given a FALSE ALL-CLEAR on precisely the question asked here, because the CBOR metadata trailer is fixed-length and a differently-compiled contract can report an identical size. The Base Sepolia record makes that point and then relies on source-blob identity instead; this record does not need to, because the bytes were compared. The codesize table below is retained only as a cross-check that a wrong CONTRACT was not deployed under a right name — a different and also useful failure.",
    "whatItDefeats": "The scrambled `transactions[].hash` column described in `broadcastLabelWarning` cannot mislabel this table. VaultFactory (3,572 B) and VaultDeployer (938 B) are the pair Sprint 9 actually swapped, and their runtimes are neither the same length nor the same bytes, so a byte-for-byte match against the named artifact identifies each of them independently of every label in the broadcast file.",
    "reproduce": "git checkout b1cde122 && cd contracts && forge build, then compare `out/<Contract>.sol/<Contract>.json` `deployedBytecode.object` with `cast code` as above, masking the artifact's own `immutableReferences` windows. Per scripts/verify-deployment-reproducibility.mjs, only the pinned sourceCommit is guaranteed to reproduce a deployment trailer-for-trailer; HEAD is not.",
    "runtimeCodesize": {
      "how": "`cast codesize <addr> --block 54991182`, compared with the same artifacts. 7 of 7 equal.",
      "onChainEqualsBuilt": {
        "OperatorRegistry": 2219,
        "SubVaultRegistry": 1605,
        "FeeEngine": 2902,
        "Governance": 12155,
        "VaultDeployer": 938,
        "VaultFactory": 3572,
        "ChainlinkOracle": 1532
      }
    },
    "vaultCoreCreationCode": {
      "claim": "The vaults this deployment will produce are byte-identical to what b1cde122 builds, and that was read off the chain rather than inferred from VaultDeployer's own runtime.",
      "how": "VaultDeployer holds VaultCore's creation code in two SSTORE2 data contracts, not in its own 938 B runtime, so its runtime says nothing about what it will deploy. `codeChunkA()` returns 0x3B15efeCB5E6520471cD31E36204182f1708c0c1 (11,196 B) and `codeChunkB()` returns 0x55442569F98c82fF9CF06616aD0826fad6a9BD50 (11,197 B); `cast code` on both, each stripped of its leading SSTORE2 STOP byte and concatenated, gives 22,391 bytes that equal `out/VaultCore.sol/VaultCore.json` `bytecode.object` EXACTLY.",
      "codeChunkA": "0x3B15efeCB5E6520471cD31E36204182f1708c0c1",
      "codeChunkB": "0x55442569F98c82fF9CF06616aD0826fad6a9BD50",
      "vaultCoreCreationCodeBytes": 22391,
      "vaultCoreRuntimeBytesWhenBuilt": 20650,
      "whyItMatters": "A singleton codesize check on VaultDeployer is silent about the bytecode of every vault it will ever create. Reading the chunks is the only on-chain evidence for that, and it is the check this deployment's currency claim actually turns on — at the time this was written no vault existed to measure instead.",
      "whatTheFirstVaultAdded": "A vault exists now, and it corroborates the chunk read rather than replacing it. `eth_getCode` on vault #1 at block 59,209,966 returns 20,650 bytes, which is exactly `vaultCoreRuntimeBytesWhenBuilt` above — a figure derived from the SSTORE2 chunks before any vault had been created. The prediction and the measurement agree. This is a SIZE comparison and therefore the weaker check that `whyThisAndNotCodesize` warns about; it is recorded as a cross-check and no byte-for-byte comparison of the vault's runtime against the b1cde122 artifact was run for this note, so none is claimed."
    }
  },

  "oracle": {
    "ChainlinkOracle": "0x79279FBa3b6F6736f07cbBFcB7Cf0559466D5bfB",
    "note": "C-6 launch model: ONE genuine Chainlink Data Feed per asset, no multi-source median and no quorum. `deployment.mjs` reads each asset's single `feed` as a one-element source set. The settlement token is pinned inside the oracle and is deliberately absent from `assets` — the constructor rejects a token that is both pinned and listed (ChainlinkOracle.sol:151). This oracle is the sole entry in the factory's oracle allowlist: `oracleAllowlistEnforced()` is true and `isAllowedOracle(0x79279FBa…)` is true, both read at block 54,991,182.",
    "usdc": "0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168",
    "usdcNote": "READ BACK from `ChainlinkOracle.usdc()`, and the token it names is USDG, not Circle USDC — `symbol()` returns \"USDG\", `decimals()` returns 6, both read at block 54,991,182. The key keeps the name `usdc` because that is the name the contract's own getter and `scripts/soak/deployment.mjs` use. `priceWad(0x5fc5360D…)` returns exactly 1e18, which is the pin, not a feed reading.",
    "maxStalenessSeconds": 86400,
    "maxStalenessNote": "A CHAIN READ ON THIS RECORD, unlike the Base Sepolia record's, which states plainly that its 86400 came from a config file because ChainlinkOracle exposes no `maxStalenessSeconds()` getter. It still does not — but staleness is a per-asset heartbeat and the heartbeat IS readable, as the second member of the public `feedOf` struct. `feedOf(WETH).heartbeat` and `feedOf(cbBTC).heartbeat` both return 86400 at block 54,991,182, so this number is what the two assets carry on chain and the value is theirs, not a configuration's. 86,400 s is `ChainlinkOracle.MAX_HEARTBEAT` exactly (ChainlinkOracle.sol:98) — the loosest bound the constructor accepts, so a price up to a day old is served without reverting. MAX_HEARTBEAT itself is a private constant and reverts when called; the ceiling is read from the source, not the chain.",
    "sequencerUptimeFeed": "0x0000000000000000000000000000000000000000",
    "sequencerUptimeFeedNote": "address(0), read back from `ChainlinkOracle.sequencerUptimeFeed()` at block 54,991,182 — an owner-approved exemption of 2026-09-04, not an omission. Chainlink publishes no L2 Sequencer Uptime Feed for chain 4663, and the Base mainnet feed is not portable (0xBCF85224fc0756B9Fa45aA7892530B47e10b6433 has zero bytes of code here). WHAT THE CONTRACT DOES WITH IT: `_requireSequencerUp` opens with `if (address(seq) == address(0)) return;` (ChainlinkOracle.sol:314), so the guard is skipped and this oracle serves prices straight through a sequencer outage rather than reverting. It fails OPEN. `GRACE_PERIOD()` reads 3600 on chain but never applies while the feed is zero. What is left carrying a bad price is the per-asset heartbeat — at its ceiling here — and the sane-price band. The exemption that let the deploy script accept a zero feed on this chain is PR #203, merged 2026-09-05T05:11:24Z and therefore present in sourceCommit b1cde122; `contracts/config/robinhood-mainnet.json` still describes #203 as open and unmerged, which was true when that file was written and is stale now. The consequence is unchanged by the deploy: the sequencer guard has still never executed against a live feed, on this chain or any other.",
    "assets": {
      "WETH": {
        "token": "0x0Bd7D308f8E1639FAb988df18A8011f41EAcAD73",
        "feed": "0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9",
        "underlyingFeed": "0x78F3556b67E17Df817D51Ef5a990cDaF09E8d3A9",
        "heartbeatSeconds": 86400,
        "scale": 10000000000,
        "minPriceWad": "100000000000000000000",
        "maxPriceWad": "100000000000000000000000",
        "readback": "`feedOf(0x0Bd7D308…)` at block 54,991,182 returns the whole struct: feed 0x78F3556b…, heartbeat 86400, scale 1e10, minPriceWad 1e20, maxPriceWad 1e23. The feed's own `description()` is \"ETH / USD\" and `decimals()` is 8. `priceWad(0x0Bd7D308…)` returned 2455221900000000000000 (~$2,455.22), inside the band. The token's `symbol()` is \"WETH\"."
      },
      "cbBTC": {
        "token": "0xCEC185eB182c47d1bA1EFc84e6959e18cd620Be4",
        "feed": "0x0009cD492adf8167f9eEBf1293556A673530a21a",
        "underlyingFeed": "0x0009cD492adf8167f9eEBf1293556A673530a21a",
        "heartbeatSeconds": 86400,
        "scale": 10000000000,
        "minPriceWad": "1000000000000000000000",
        "maxPriceWad": "1000000000000000000000000",
        "readback": "`feedOf(0xCEC185eB…)` at block 54,991,182 returns feed 0x0009cD49…, heartbeat 86400, scale 1e10, minPriceWad 1e21, maxPriceWad 1e24. The feed's `description()` is \"CBBTC / USD\" and `decimals()` is 8. `priceWad(0xCEC185eB…)` returned 79551981378520000000000 (~$79,551.98), inside the band. The token's `symbol()` is \"cbBTC\"."
      }
    },
    "constructorArgs": "Recovered from the chain, not from the broadcast file: `cast tx 0x13162f0b… input` minus the artifact's creation-code prefix leaves 704 bytes of ABI encoding, which decodes to assets [WETH, cbBTC], feeds [0x78F3556b…, 0x0009cD49…], heartbeats [86400, 86400], minPriceWad [1e20, 1e21], maxPriceWad [1e23, 1e24], usdc 0x5fc5360D…, sequencerUptimeFeed address(0). Every one of those matches the readback above."
  },

  "execution": {
    "AggregationRouterAdapter": null,
    "adapterNote": "NO ADAPTER IS DEPLOYED ON THIS CHAIN, and that is the mainnet design rather than an unfinished step. `contracts/script/Deploy.s.sol:33` states it directly — oracles and execution adapters are not protocol singletons, each vault creator supplies its own — and the only script in the repository that deploys an `AggregationRouterAdapter` is `DeployTestnet.s.sol`, which was not run here. The ten broadcast transactions contain no adapter. The field is null rather than absent so that a reader looking for it finds this note instead of an ambiguity, and the router address below is deliberately NOT written here: a router and an adapter are different contracts, and putting one under the other's key would be exactly the mislabeling `scripts/soak/deployment.mjs` was written to defend against. CONSEQUENCE FOR THE PARSER, stated so it is not discovered as a surprise: `parseDeployment` requires `execution.AggregationRouterAdapter` to be a 20-byte address, so it throws on this record — `deployment: AggregationRouterAdapter is not a 20-byte address: null` — and it is the ONLY field it throws on. The parser assumes the testnet-shaped deployment it was written against. Nothing here should be changed to make it pass, and neither should the parser: a soak run started against an adapterless record would fail later and less legibly.",
    "adapterNoteOvertaken": "THE FIRST SENTENCE OF `adapterNote` IS NO LONGER TRUE OF THIS CHAIN, and the `AggregationRouterAdapter` key above is left null because that is still the honest answer to the question it asks — this DEPLOYMENT deploys no adapter, and its ten transactions still contain none. What has changed is the wider claim. AN ADAPTER CONTRACT EXISTS ON CHAIN 4663 AND VAULT #1 IS BOUND TO IT. Measured at block 59,209,966: vault #1's `createVault` calldata carried `allowedAdapters` [0xc83B9CE8a12B8aca3f5f7d1C20383d60B1ECaA5E]; `VaultCore.isAllowedAdapter(0xc83B9CE8…)` on the vault returns true; that address has 1,981 bytes of runtime; and its `router()` returns 0xcaf681a66d020601342297493863e78c959e5cb2, which is exactly the router recorded under `router` above. `isAllowedAdapter(0xcaf681a6…)` returns false, so the vault is bound to the adapter and not to the router directly, which is the expected shape. WHAT THIS RECORD DOES NOT ESTABLISH, and nobody should read into it: which source that adapter was built from, whether it is this repository's `AggregationRouterAdapter`, who deployed it, or whether it has been reviewed. Those are not answerable by the reads above and no byte-level comparison was run for it, so none is claimed. `VaultCore`'s adapter allowlist is constructor-only (VaultCore.sol:229 calls it fixed forever, set at :247-248 and enforced at :895), so this binding is immutable for vault #1 exactly as its creator is. IT IS AN OPEN ITEM: an execution surface on a live mainnet vault holding member funds, whose provenance this address book cannot vouch for.",
    "router": "0xcaf681a66d020601342297493863e78c959e5cb2",
    "routerName": "Uniswap SwapRouter02 (Robinhood Chain mainnet). Not at the canonical cross-chain SwapRouter02 address 0x68b3465833fb72A70ecDF485E0e4C7bD8665Fc45, which has zero bytes of code on 4663.",
    "routerAllowedSignatures": [
      "exactInputSingle((address,address,uint24,address,uint256,uint256,uint160))",
      "exactInput((bytes,address,uint256,uint256))"
    ],
    "routerNote": "Read at block 54,991,182: the router has 24,497 bytes of runtime, its `factory()` returns 0x1f7d7550B1b028f7571E69A784071F0205FD2EfA, and the two allow-listed signatures' selectors — 0x04e45aaf for `exactInputSingle` and 0xb858183f for `exactInput`, both confirmed with `cast sig` — each appear in that runtime. The router is recorded here as the venue a future adapter would be built against; NOTHING IN THE TEN TRANSACTIONS ABOVE REFERENCES IT. This note used to continue that no adapter existed to reference it and no vault existed to be bound to one; both halves of that are overtaken. An adapter at 0xc83B9CE8a12B8aca3f5f7d1C20383d60B1ECaA5E returns this exact router address from its own `router()` getter, and vault #1's constructor-fixed allowlist admits that adapter. See `adapterNoteOvertaken`."
  },

  "infrastructure": {
    "usdc": "0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168",
    "usdcNote": "The same address `ChainlinkOracle.usdc()` returns, repeated under the key `scripts/soak/deployment.mjs` reads. It is USDG (Global Dollar), symbol \"USDG\", 6 decimals — read on chain, not assumed from the key name."
  },

  "verifiedWiring": {
    "note": "Every row read from chain 4663 with `cast call --block 54991182` against the addresses in `singletons`. The four REVERSE-direction edges are listed first and deliberately: a forward read can agree with an address book that is itself wrong, and only the reverse edges catch that. Together the eleven rows close the loop in both directions between all six singletons.",
    "registry.factory()": "0xc44B853F037b4fF33B831C9a2B341686dEC88Fd1",
    "registry.feeEngine()": "0x221D09326DBf6CDb708E7aBEdC9B117d64Ac4232",
    "subReg.factory()": "0xc44B853F037b4fF33B831C9a2B341686dEC88Fd1",
    "feeEngine.registry()": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568",
    "gov.subVaultRegistry()": "0x692385262C05df7515560886f167c4eDD0814025",
    "factory.registry()": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568",
    "factory.governance()": "0x790A308f1ac06FeD4C79884BAD25d0C721C5B125",
    "factory.feeEngine()": "0x221D09326DBf6CDb708E7aBEdC9B117d64Ac4232",
    "factory.subVaultRegistry()": "0x692385262C05df7515560886f167c4eDD0814025",
    "factory.vaultDeployer()": "0xc36198FD2c7C62738159ED1FF965679105FAF05a",
    "factory.oracleAllowlistEnforced()": true,
    "factory.isAllowedOracle(ChainlinkOracle)": true,
    "factory.allowSubVaults()": false,
    "factory.vaultCount()": 0,
    "factory.vaultCount()Note": "THIS ROW IS SUPERSEDED AS A STATEMENT ABOUT TODAY, and it is deliberately left at 0. Every row in this block is pinned to block 54,991,182 and 0 is what the factory returned there; rewriting it to the current answer would falsify the note above it and destroy the one-consistent-state property the whole table rests on. The current answer is 1. Vault #1 exists and holds funds: see `smokeVault` below, read 2026-09-10 at block 59,209,966. A reader who wants the count as of now reads that object and not this row."
  },

  "allowSubVaultsNote": "`factory.allowSubVaults()` RETURNS FALSE — read from chain 4663 at block 54,991,182, and corroborated independently by the constructor arguments recovered from `cast tx 0x655991c7… input`, whose sixth word is zero. This is the launch parameter, and it is the observed state of a live deployment rather than an intention: sub-vaults are off, so this deployment is root-vaults-only, which is what closes C-1 (docs/LAUNCH-READINESS.md gate 0). It is the opposite of the Base Sepolia record's TRUE, and that asymmetry is deliberate there — that testnet keeps sub-vaults enabled because the SV-7 look-through soak drill needs a child vault to exercise. Do not read either value as drift from the other. The factory constructor takes this flag once and the factory is immutable, so it cannot be changed on this deployment.",

  "ownershipNote": "MEASUREMENT: `owner()` reverts on all six singletons and on the oracle, called at block 54,991,182 — these contracts expose no such getter. Recorded as an observation about what was read, and it is not by itself a description of what any party can or cannot do; the authority questions belong to the source and to docs/LAUNCH-READINESS.md, not to an address book.",

  "intendedCreator": {
    "safe": "0xC73Bd58725afF051109b97B7Be40a8E31C6CAD4c",
    "readback": "Read at block 54,991,182: 171 bytes of code, `VERSION()` \"1.4.1\", `getThreshold()` 1, `getOwners()` [0x0f80606a2283fD9C67cE2eEC79B90E95907F9f35]. A threshold-1 single-owner Safe is a 1-of-1 — an address with a different failure surface from the EOA behind it, not a multisig, and it does not by itself satisfy an operational gate asking for shared custody.",
    "why": "`VaultFactory.createVault` is permissionless and `msg.sender` becomes both the vault's creator and its attested operator (VaultFactory.sol:183-184, attestation at :195); `VaultCore.creator` is immutable (VaultCore.sol:73); and `VaultParams` carries no field for either identity (VaultFactory.sol:172-181). So the creating account is fixed at creation and cannot be corrected afterwards. The Base Sepolia smoke vault was created by the deployer EOA and that record states the consequence: the first vault here must be created BY THE SAFE, not by a script from the EOA.",
    "whyOvertaken": "THAT DID NOT HAPPEN, and the reasoning above is now a record of an intention rather than a description of the chain. Vault #1 was created by the deployer EOA 0x0f80606a…, not by this Safe — `smokeVault.creator` above, read back from `VaultCore.creator()` and corroborated by `OperatorRegistry.operatorAddressOf(1)` and by the `VaultCreated` log's own creator topic. The immutability argument in `why` is unchanged and correct; it is what makes the outcome permanent instead of correctable. `creatorIdentityNote` above enumerates what that means. This block is kept rather than deleted because it is the reason the outcome matters.",
    "note": "The deployer EOA 0x0f80606a… broadcast all ten transactions above and has zero bytes of code. Deploying the singletons is not creating a vault; nothing in those ten transactions binds the EOA to any vault identity.",
    "noteOvertaken": "The last clause was true of the ten transactions above and is no longer true of this deployment as a whole. A later, eleventh transaction — 0x6f895c3a… in block 58,991,819, which is not part of the deploy broadcast — bound that EOA to vault #1 as both its immutable creator and its attested operator. The sentence above has been narrowed to the ten transactions it was actually about rather than deleted, because the distinction it draws (deploying singletons is not creating a vault) is still the correct one."
  },

  "smokeVault": {
    "address": "0x9b0229FF0613EaD59e41Eec556e03b5ED228e2b4",
    "createdInBlock": 58991819,
    "createdByTx": "0x6f895c3ab23338e36836ca90ca70c00b471b6328a57b94f3074abf7083d8024f",
    "creator": "0x0f80606a2283fD9C67cE2eEC79B90E95907F9f35",
    "runtimeCodesize": 20650,
    "readBackAt": {
      "block": 59209966,
      "how": "`eth_call` with the block pinned to 59209966 against https://rpc.mainnet.chain.robinhood.com, the same endpoint the rest of this record names. `eth_chainId` on it answers 4663. Every row in `state` below is one consistent state at that block, on the same discipline the deploy table uses. NOT read with `cast`: this endpoint serves no archive state — an `eth_call` at the deploy block 54,989,195 answers `metadata is not found`, so a historical re-read of the rows above is not available here and only the head can be sampled.",
      "date": "read 2026-09-10",
      "blockTimestampUtc": "2026-09-10T07:03:26Z"
    },
    "state": {
      "factory.vaultCount()": 1,
      "factory.allVaults(0)": "0x9b0229FF0613EaD59e41Eec556e03b5ED228e2b4",
      "creator()": "0x0f80606a2283fD9C67cE2eEC79B90E95907F9f35",
      "totalShares()": "20000000000000000000",
      "navWad()": "20000000000000000000",
      "idleUsdc()": "20000000",
      "capacityCapUsdc()": "50000000000",
      "minDepositUsdc()": "10000",
      "holderCount()": 1,
      "nonCreatorMemberCount()": 0,
      "sharesOf(creator)": "20000000000000000000",
      "totalQueuedShares()": "0",
      "exitFeeMaxBps()": 50,
      "exitFeeDecayPeriod()": 604800,
      "basketLength()": 2,
      "parentVault()": "0x0000000000000000000000000000000000000000",
      "usdc()": "0x5fc5360D0400a0Fd4f2af552ADD042D716F1d168",
      "oracle()": "0x79279FBa3b6F6736f07cbBFcB7Cf0559466D5bfB",
      "governance()": "0x790A308f1ac06FeD4C79884BAD25d0C721C5B125",
      "feeEngine()": "0x221D09326DBf6CDb708E7aBEdC9B117d64Ac4232",
      "operatorRegistry()": "0xE200d63DB7c665F8eead3C7BDF3f0c030d7a6568"
    },
    "unitsNote": "THE SETTLEMENT TOKEN IS USDG, NOT CIRCLE USDC — the getter names say `Usdc` because that is what the contract calls them (see `usdcNote`). So `idleUsdc()` 20000000 is 20 USDG at 6 decimals, `capacityCapUsdc()` 50000000000 is 50,000 USDG, and `minDepositUsdc()` 10000 is 0.01 USDG. The vault's actual token balance agrees with its own accounting: `USDG.balanceOf(0x9b0229FF…)` returns 20000000 at the same block, so the 20 USDG is in the contract and not merely booked. `navWad()` and `totalShares()` are both 20000000000000000000 (2e19), i.e. 1e18 of share per USDG at the opening ratio.",
    "runtimeCodesizeNote": "20,650 bytes, `eth_getCode` at block 59,209,966. That is EXACTLY `bytecodeCurrency.vaultCoreCreationCode.vaultCoreRuntimeBytesWhenBuilt`, which was derived before any vault existed by reading VaultDeployer's two SSTORE2 chunks. The chunk read predicted the size of a vault that had not been created; the vault now exists and measures the predicted size. A size match is a cross-check and not a byte-identity proof — `bytecodeCurrency.whyThisAndNotCodesize` says why, and that caveat is unchanged.",
    "creationNote": "Established read-only, without an archive node and without trusting any broadcast file. `eth_getLogs` on the factory for `VaultCreated(address,address,address,uint256)` (topic0 0x4dda9a6d0ba03769e9813c47681795a7210f951e6ef31e64772e13b9ea0f1406) over the whole range from the deploy block to the head returns EXACTLY ONE log: vault 0x9b0229FF…, creator 0x0f80606a…, usdc 0x5fc5360D…, capacityCapUsdc 0xba43b7400 (50,000,000,000). Its transaction is 0x6f895c3a… in block 58,991,819 (block timestamp 1789001789 = 2026-09-10T00:56:29Z), receipt status 1, txIndex 1, gasUsed 4,616,278, sent from 0x0f80606a…, to the factory, selector 0x49af0336 — which is `createVault((address,address[],address,uint256,uint256,uint256,uint256,address[]))`. One log across the full range is also the independent corroboration that the count of 1 is a count of this vault and not of some other.",
    "createVaultParams": "DECODED FROM THE TRANSACTION'S OWN CALLDATA, not from a config file: usdc 0x5fc5360D…, basketAssets [0x0Bd7D308… (WETH), 0xCEC185eB… (cbBTC)], oracle 0x79279FBa… (the blessed one), capacityCapUsdc 50000000000, minDepositUsdc 10000, exitFeeMaxBps 50, exitFeeDecayPeriod 604800, allowedAdapters [0xc83B9CE8a12B8aca3f5f7d1C20383d60B1ECaA5E]. Every one of those that has a getter reads back equal in `state` above. The adapter entry is the one that contradicts this record elsewhere — see `execution.adapterNote`.",
    "depositNote": "The 20 USDG arrived in two transactions, both from 0x0f80606a… and both to the vault, and both are in the vault's complete log history — `eth_getLogs` on the vault from its creation block to 59,209,966 returns exactly these two. (1) `DepositPending(member, 20000000, availableAt 1789002766)`, tx 0x1b60757d5c7eed570e8ba0954362109e1b9de8bf9706e2b44906061530ed6fe5, block 59,021,153, status 1, gasUsed 164,489. (2) `DepositActivated(member, 20000000, sharesMinted 20000000000000000000)`, tx 0xe171b18b7db79d7ce709075806ff375686bdd090b1eb07a21b19a9264aab811f, block 59,164,671, status 1, gasUsed 324,589. The two-step shape is the deposit window, not a retry.",
    "note": "VAULT #1 EXISTS AND HOLDS REAL MONEY. Twenty USDG is a small number and this record does not dress it up as anything else, but it is a live mainnet vault with a funded member position, and every surface in this repository that said otherwise was describing a state that ended at block 58,991,819. WHAT THIS RECORD DOES NOT SETTLE: whether the amount and the parameters were chosen as a deliberate smoke test. The evidence is consistent with one — 20 USDG against a 50,000 USDG cap is 0.04% of capacity, and the creator is the sole holder — but intent is not readable from the chain and nothing here asserts it. WHAT IT DOES SETTLE, because it is permanent: the creator is the deployer EOA and not the Safe. See `creatorIdentityNote`."
  },
  "creatorIdentityNote": "THE CREATOR OF VAULT #1 IS AN EOA, 0x0f80606a2283fD9C67cE2eEC79B90E95907F9f35 (zero bytes of code at block 59,209,966), and NOT the Safe 0xC73Bd58725afF051109b97B7Be40a8E31C6CAD4c that `intendedCreator` called for. THIS CANNOT BE CHANGED BY ANY TRANSACTION. `VaultFactory.createVault` passes `msg.sender` into the vault as `creator_` (VaultFactory.sol:245) and into the registry as the attested operator (VaultFactory.sol:195); `VaultCore.creator` is `address public immutable` (VaultCore.sol:73), assigned once in the constructor (VaultCore.sol:274); and `VaultParams` carries no field for either identity (VaultFactory.sol:172-181). Read back on chain rather than inferred: `OperatorRegistry.operatorOf(0x9b0229FF…)` is opId 1, `operatorAddressOf(1)` is 0x0f80606a…, and `operatorIdOf(0xC73Bd587…)` is 0 — the Safe is not a registered operator on this chain at all. WHAT THE CREATOR IDENTITY DOES CONFER, enumerated so the list can be checked item by item rather than taken on trust. (1) THE FEE STREAM. `FeeEngine.onFeeCollected` and `onFeeCollectedAsset` credit `claimableFees[registry.operatorAddressOf(opId)]` (FeeEngine.sol:104, :120) and `claimFees` pays `claimableFees[msg.sender]` to its caller, so this vault's 10% performance fee is claimable by that EOA and by nobody else, permanently. (2) NOTHING ELSE INSIDE THE VAULT. `VaultCore` contains no function gated on the creator: every privileged entry point it has requires `msg.sender == address(governance)` (VaultCore.sol:781, :819, :894). The creator identity appears in `VaultCore` in exactly one other place, and it is a CONSTRAINT rather than a power — `_checkCreatorGate` (VaultCore.sol:596-603) blocks the creator from taking its stake below `CREATOR_MIN_STAKE_BPS` (500 = 5%) while other members remain. (3) IT CANNOT BE REBOUND. `OperatorRegistry.attestVault` is the only writer of `_vaultOperator` (OperatorRegistry.sol:106) and is factory-only (:103); `registerOperator` refuses an address that already has an id (:92) and `operatorAddressOf` is written only there (:95). No path exists to point either identity at the Safe. WHAT THE ACTUAL EXPOSURE IS IF THAT KEY IS LOST OR COMPROMISED, measured rather than imagined: at block 59,209,966 that EOA holds `sharesOf` 20000000000000000000 of a `totalShares()` of the same number — the entire vault — against a `navWad()` of 2e19 and 20 USDG of assets. So whoever holds the key can exit the whole 20 USDG, and that plus this vault's future fee credits is the whole of what the key reaches on this deployment. It reaches no further: `holderCount()` is 1 and `nonCreatorMemberCount()` is 0, so there are no other members' funds in this vault to move; `ownershipNote` above records that `owner()` reverts on all six singletons and the oracle, so there is no protocol-wide administrative position attached to it; and `Governance.propose` gates on stake rather than on operatorship, so the identity buys no proposal right that the shareholding does not already buy. NOTE ALSO, because it follows from `nonCreatorMemberCount()` being 0, that the 5% gate in (2) is not binding today: its condition is `member == creator && nonCreatorMemberCount > 0`, so with no other members the creator is not currently held to the 5% floor. THE ONE THING THIS DOES SETTLE FOR THE FUTURE: `intendedCreator` below was written to make vault #1 the Safe's, and that opportunity is spent. Any later vault whose operator payout must sit behind the Safe has to be a NEW vault created by the Safe; this one cannot be converted into it.",

  "lifecycleNote": "NO TEN-PHASE LIFECYCLE RUN HAS BEEN EXECUTED AGAINST THIS DEPLOYMENT, and that part is unchanged: the evidence in docs/evidence/testnet-lifecycle-run.json belongs to the Base Sepolia deployment and to no other. THE REST OF THIS NOTE WAS OVERTAKEN. It used to say that with no vault and no adapter here, no deposit, rebalance, fee accrual or exit had been exercised on chain 4663. A vault and an adapter both exist now, and a deposit HAS been exercised: `smokeVault.depositNote` records the two transactions that put 20 USDG into vault #1 and minted 2e19 shares against it, both with receipt status 1. What this record does NOT claim is any of the other three. It states no fee accrual and no exit, and it deliberately reports nothing about rebalancing — see `governanceActivityNote`.",
  "governanceActivityNote": "A GOVERNANCE ROUND WAS IN FLIGHT AT THE MOMENT THESE ROWS WERE READ, AND THIS RECORD DELIBERATELY DOES NOT REPORT ITS RESULT. What was read, at block 59,209,966 (2026-09-10T07:03:26Z): `Governance.proposalCount()` returns 1, and `Governance.proposals(1)` returns vault 0x9b0229FF… — vault #1 — proposer 0x0f80606a…, ptype 0, actionHash 0x5c33178ab655f450324f624e38fc7874bf4e808afdfbe5e8d474d83ccde2e8c6, createdAt 1789019237 (2026-09-10T05:47:17Z), with a commit window closing at 2026-09-10T06:47:17Z and a reveal window closing at 2026-09-10T07:47:17Z. The read instant sits inside that reveal window, so the round had not finished being executed when it was sampled and any statement here about how it ended would be a guess written into an address book. The proposal's status field, its executable and expiry timestamps, and the vault's post-round balances are therefore omitted rather than recorded as zero or as anything else — a zero in this record would read as a finding. WHOEVER UPDATES THIS FILE NEXT owns recording the outcome, from the chain, after the round has settled. Note also that `Governance.propose` gates on stake and not on operatorship, and that this proposal's `memberCount` snapshot is 1 — a single-member electorate, which is not the regime any weighting language in this repository is written about.",

  "x402Note": "NO x402 ON THIS DEPLOYMENT (owner, 2026-09-05). None of the ten transactions deploys or configures an x402 payment surface, no contract recorded above implements one, and nothing in this deployment depends on one. Recorded explicitly because the absence is a decision rather than an oversight.",

  "explorerVerificationNote": "ALL SEVEN CONTRACTS ARE SOURCE-VERIFIED, on Sourcify and on the Blockscout explorer, and both were read back for this note rather than carried over. SOURCIFY: `GET https://sourcify.dev/server/v2/contract/4663/<address>` returns `creationMatch` exact_match AND `runtimeMatch` exact_match for every one of the seven. The creation check and the runtime check are separate matches and both are exact, which is the strongest answer that endpoint gives. EXPLORER: `GET https://robinhoodchain.blockscout.com/api/v2/smart-contracts/<address>` returns `is_verified` true and `is_partially_verified` false for every one of the seven. Both sweeps were run again on 2026-09-05, contract by contract, with a browser User-Agent. AGAINST WHICH SOURCE: the tree at `sourceCommit` b1cde122, the same commit `bytecodeCurrency` above pins, compiled with solc 0.8.26+commit.8a97fa7a, which is the `compiler_version` the explorer reports for all seven. THE LICENCE HEADER ON THE VERIFIED SOURCE IS BUSL-1.1, in all 27 source files across the seven contracts, read out of Sourcify's own `sources` payload. That is the header the code was compiled and broadcast with. The repository relicensed from BUSL-1.1 to MIT afterwards, on 2026-09-05, in PR #223, so the repository today and the verified source deliberately disagree: an explorer shows the source a deployment was built from, and that source cannot change after the fact. Do not \"fix\" the explorer to match the repository. The explorer's own `license_type` field reads \"none\" for all seven, which is a field nobody set rather than a licence claim. A WARNING FOR WHOEVER READS THIS BACK: the explorer's `/api/v2/smart-contracts/` endpoint answers HTTP 500 intermittently on contracts that are verified. VaultDeployer needed several attempts, and while it was still failing, `/api/v2/search?q=<address>` reported `is_smart_contract_verified` true and `/api/v2/addresses/<address>` reported `is_verified` true for the same address. A 500 from that endpoint is a server error, NOT an answer that a contract is unverified; retry it or read one of the other two. This note supersedes the earlier state in which an automated client was refused with HTTP 403 behind a bot challenge and `forge verify-contract` failed at the ABI lookup before submitting; that challenge was never worked around. How verification was ultimately accepted is not something these read-backs establish, and nothing here should be read as asserting a mechanism. WHAT HAS NOT CHANGED: source verification is an explorer convenience and is still not what establishes the identity of these contracts. `bytecodeCurrency` above is, by byte-for-byte comparison against the b1cde122 artifacts, and it depends on no explorer. What verification adds is that a reader who does not build the tree can read the source beside the bytecode. docs/DEPLOYMENT.md carries the same three facts and the two endpoint URLs."
}
