{
  "name": "index-vault-protocol",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:contracts": "cd contracts && forge build",
    "test:contracts": "cd contracts && forge test -vv",
    "test:backend": "node --test packages/indexer/test/*.test.mjs apps/api/test/*.test.mjs"
  }
}