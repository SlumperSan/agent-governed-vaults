# HANDOFF — 2026-07-31 (session limit reached)

**Read `ORG-CHARTER.md` first, then this, then `ORG-BACKLOG-INDEX.md`.** Session limit hit at ~92%;
resets 12:50am America/Chicago. Nothing is half-written. One agent died mid-task (§4).

---

## 1. The single most important open question

**Is the x402 economy ~$63k/month or ~$52 MILLION?** We do not know, and Michael must not make a
strategy call until we do.

| Source | Claim |
|---|---|
| CDP Bazaar catalog (measured by us, full sweep) | 373,056 calls/30d · **$62,801 derived GMV** |
| x402scan `/facilitators/stats` (paid $0.01) | **198,301,108 tx · ~$52.6M** · 847k buyers |
| x402scan `/resources` (paid $0.01, same vendor) | busiest resource = **3,540 calls** |

x402scan's own two endpoints disagree with each other by ~1000x. Full detail and the leading
hypothesis in `notes-x402scan-paid-2026-07-31.md`.

**I previously advised Michael the market was tiny and not worth building for income. That advice
came only from CDP's catalog. It may be wrong by orders of magnitude.** Settling this is the highest
priority on the board.

**How to settle it — needs nobody's permission:** the paid calls bought us **every facilitator's
on-chain settlement addresses** (40 Coinbase Base + 7 Solana, plus 9 other facilitators). Query Base
RPC (`https://mainnet.base.org`, no key) for USDC Transfer events to those addresses and count
directly. USDC on Base = `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`, 6 decimals.

---

## 2. Full sweep COMPLETE — 15,524 routes probed (run 6)

Our own measurement, not CDP's claim:

- **96.6% alive** (14,990 valid x402 challenges). Latency median 99ms, p90 423ms.
- **4.8% price mismatch** catalog vs live (726 of 14,988 comparable).
- **19.3% spec violations** (2,997 routes). Top: 1,554 decimal-not-atomic amounts, 1,368 declaring a
  bazaar extension in the catalog but emitting none on the wire.
- **474 routes where the catalog's x402 version disagrees with the wire.**
- v2 header 13,636 · v1 body 1,346 — both live in the wild.
- 1,577 distinct hosts (the honest "number of services"; 15,524 is routes).

### The headline finding — this is a safety story, not a data-quality one

> `https://claudelines.com/api/download` — catalog says **$0.05**, live 402 demands **$1,000,000**.
> A 20,000,000× discrepancy.

Others: `citysquatters.com` $0.01 → **$10,000**. `govparse.io` $0.05 → **$25** (×500).
`stablepeopledata.dev` $0.28 → **$28** (×100).

**An agent that trusts Coinbase's catalog price and auto-pays can be drained.** That is the sharpest
possible argument for measured-over-declared, and nobody else is publishing it.

### builder-code concentration (the referral rail, 17.2% of routes)

`bc_gxy6qn5p` spans **1,587 routes across 144 hosts** — one entity with a tenth of the catalog.
`bc_b4aw4uzd`: 524 routes but only 1 host. `bc_hc2dhq09`: 62 routes, 9,624 calls (highest traffic).

---

## 3. State

- **Sweep:** complete, 15,524 probes stored, run 6. Nothing running.
- **`prune.py`:** launched headless after the sweep finished — check `logs/prune.log`. Expect
  ~199MB → ~120MB (67.4% of `raw_json` was byte-identical duplication).
- **Daily snapshot:** scheduled task `x402-daily-snapshot` registered, 3:08am local, headless.
  Michael explicitly authorized it. It runs fetch → probe(400 sample) → prune → report → commit.
- **Git:** everything committed. DB and logs gitignored.
- **Spend so far: $0.03** (three x402scan calls, each Paybox-approved by Michael).

---

## 4. What died and needs redoing

**On-chain verification agent — killed by the session limit before writing anything.** Its brief is
§1 above. `notes-onchain-verification.md` does not exist. Re-dispatch it first thing.

**A 4th paid call was queued and may or may not have been approved:** x402scan `/facilitators?page=1`
(page 2 of the facilitator list — page 0 had `has_next_page: true`). Approval URL
`https://app.paybox.sh/approvals/ca2d3473-4203-48a9-b7c6-a0bbd0be99b2`, request_id
`0175bb6d-dd67-4c02-bff9-19e58d7affeb`. **Poll `get_request` with that id before re-paying** — do not
spend another cent until you know whether it settled.

---

## 5. Traps — do not rediscover

- **Council fires agents; the orchestrator decides.** Before approving any firing, check the failure
  channel. A safety-blocked or session-limited agent looks identical to a lying one from downstream.
  This already cost a wasted rebrief — see `ORG-LESSONS.md`.
- **Never run two things that write the DB.** Sweep, fetch and prune all take write locks. Agents
  doing analysis must open read-only: `sqlite3.connect('file:x402_index.db?mode=ro', uri=True)`.
- **Everything headless.** `pythonw.exe` + `DETACHED_PROCESS`, or `nohup ... > log 2>&1 &`. A visible
  console got a whole work phase killed.
- **Managers and workers are haiku now** (session-limit downgrade). Briefs must name the files, the
  method and the expected output shape — do not rely on inference. Audit Council stays sonnet/high.
- **Overshooting the CDP catalog `offset` silently re-serves the last page**, and `pagination.total`
  drifts mid-fetch. Use the explicit `is_complete` flag, never `rows_stored >= total`.
- **Paid x402 calls need Michael's Paybox approval every time.** Auto mode was switched off so the
  approval prompt reaches him. Never assume approval; always surface the approval_url.

---

## 6. Next three actions, in order

1. **Re-dispatch on-chain verification** (§1). Everything else is downstream of the answer.
2. **Poll the pending payment** (§4) before spending again.
3. **Cycle 2**: departments with real queued work are Product (public site), API/MCP (the read API
   Michael named as the endgame), and Intel (the price-mismatch leaderboard — §2's $1,000,000 finding
   is the launch story). `ORG-BACKLOG.md` has the full queue.
